# Image Releases

Product repositories publish product images:

```text
openclaw-jitech -> OpenClaw product image
hermes-jitech   -> Hermes product image
```

This repository publishes wrapper images from product image digests. Server
state records the wrapper digest used by each lane.

Tags are labels. Digests are deployment identity.

